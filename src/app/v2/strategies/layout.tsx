export const metadata = {
  title: "Investment Strategies — Dealsletter",
  description:
    "BRRRR, Fix & Flip, Buy & Hold, and House Hack — every real estate investment strategy analyzed with AI-powered precision.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
