export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Create Account — Dealsletter",
  description: "Create your free Dealsletter account",
};
