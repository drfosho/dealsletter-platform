export const metadata = {
  title: "Pricing — Dealsletter",
  description:
    "Simple pricing for AI-powered real estate analysis. Start free, upgrade when your deals demand it.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
