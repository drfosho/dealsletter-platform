export const metadata = {
  title: "Contact — Dealsletter",
  description:
    "Get in touch with the Dealsletter team. We read every message and reply within 1-2 business days.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
