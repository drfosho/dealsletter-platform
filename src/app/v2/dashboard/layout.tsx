export const metadata = {
  title: "Dashboard — Dealsletter",
  description: "Your real estate analysis dashboard",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
