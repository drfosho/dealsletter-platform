export const metadata = {
  title: "My Account — Dealsletter",
  description: "Manage your Dealsletter account, subscription, and billing.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
