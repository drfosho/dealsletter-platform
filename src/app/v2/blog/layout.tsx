export const metadata = {
  title: "Blog — Dealsletter",
  description:
    "Real estate market analysis, deal breakdowns, tax strategy, and investment intel for serious investors.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
