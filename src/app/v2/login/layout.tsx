export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Sign In — Dealsletter",
  description: "Sign in to your Dealsletter account",
};
