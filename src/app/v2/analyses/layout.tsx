export const metadata = {
  title: "Analysis History — Dealsletter",
  description: "View and manage all your saved property analyses.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
